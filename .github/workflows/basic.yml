name: vanakkam

on: 
  push:
    branches:
      - main
      
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: one
        run: |
          echo "hi naavandhu one solanum "
          echo "aprem solren"
          echo "idhu push settings"
